{
  "name": "@creatorai/automation",
  "version": "0.1.0",
  "private": true,
  "description": "AI Automation Engine — Master Agent, Research, Content Planning, Workflow Management",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "tsc --project tsconfig.json --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "dependencies": {
    "@creatorai/shared": "workspace:*",
    "@creatorai/agents": "workspace:*",
    "@creatorai/providers": "workspace:*",
    "@creatorai/orchestrator": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
