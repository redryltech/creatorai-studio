{
  "name": "@creatorai/database",
  "version": "0.1.0",
  "private": true,
  "description": "Database access layer — Firestore repositories and Storage service",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "tsc --project tsconfig.json --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@creatorai/shared": "workspace:*",
    "firebase-admin": "^12.3.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
